{
  "title": "JavaScript 数组方法",
  "description": "JS 数组常用操作方法。回调统一用箭头函数，数组变量名统一为 arr",
  "category": "JavaScript",
  "content": [
    { "type": "text", "tips": "用 map 把 arr 每个元素 x 乘以 2", "content": "arr.map(x => x * 2)" },
    { "type": "text", "tips": "用 filter 保留 arr 中 x 大于 0 的元素", "content": "arr.filter(x => x > 0)" },
    { "type": "text", "tips": "用 reduce 从 0 开始累加 arr，回调参数为 (a, b)", "content": "arr.reduce((a, b) => a + b, 0)" }
  ]
}
